Algoritmo "Q.4"

Var

     // QUEST�O 4 {
     
       base, expoente, resultado, c: real
     
     // QUEST�O 4 }
     
Inicio

c <- 0  // INICIA O LOOP

escreval("C�lculo de potencias")
                                  escreval
escreva("Escreva a base: ")
                                  leia(base)
                                  escreval
escreva("Escreva a pot�ncia: ")
                                  leia(expoente)
limpatela

escreval("O resultado de", base, " elevado a",expoente," �: ")
escreval

enquanto (c = 0) faca

  resultado <- exp(base, expoente)
  
c <- 1  // SAIR DO LOOP
  
fimenquanto

escreval(resultado)

Fimalgoritmo
